import XCTest
@testable import CloudKitEncryptable

final class CloudKitEncryptableTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CloudKitEncryptable().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
